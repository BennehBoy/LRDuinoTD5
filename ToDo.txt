Autorotate sensors options - menu option, when selected the display changes to the next sensor every 5 seconds.

Fix F4 datalogging

Default sensor setting - user can select which sensor is the default at startup.

Save settings - to flash ? to SD ?

Read settings - from flash ? from SD ?

Add fuel pressure sensor

Sensor Order - user can move sensors left / right in the viewing order.

